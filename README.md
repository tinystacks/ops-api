# ops-backend
REST API for ops console

To install dependencies:

```bash
npm ci
```

To run on your local machine:

```bash
npm run clean-build;
npm start;
```

To run in docker:
```bash
npm run dev
```