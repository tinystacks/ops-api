
import { Console } from '@tinystacks/ops-model';

describe('widget client tests', () => {
  it('CONFIG_PATH', () => {
    const a: Console = {
      providers: {},
      widgets: {},
      dashboards: {},
      name: ''
    };
    expect(10).toBe(9+1);
  });
});